<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- Dummy package (won’t resolve, but Renovate will still look for it in FakePrivateFeed) -->
    <PackageReference Include="MyPrivateLib" Version="1.0.0" />
  </ItemGroup>

</Project>
